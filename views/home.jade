extends layout

block content
    include menu
    .head
        h1 Your favorite channels
        .new-channel.btn Create New Channel
    if channels.length > 0
        .channels
            each channel in channels
                div.channel(id=channel.id)
                    h2= channel.name
                    p= channel.description
    else
        h2 Нет каналов